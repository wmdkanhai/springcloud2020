server:
  port: 8006
spring:
  application:
    name: cloud-provider-payment

  cloud:
    #Consul注册中心地址
    consul:
      host: 127.0.0.1
      port: 8500
      discovery:
        service-name: ${spring.application.name}
